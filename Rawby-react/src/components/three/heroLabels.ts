// Captions for the cycling hero objects. Kept three-free so light
// components (AuthLayout) can import labels without pulling in `three`.
export const HERO_LABELS: Record<string, string> = {
  camera: "Roll camera",
  clapper: "Scene one, take one",
  typewriter: "Write the scene",
};
